<main class="dashboard">

    {{#each dashboardSections}}

        <section class="dashboard-card">
            <h2>{{title}}</h2>
            <p>{{description}}</p>
            <a class="button" href="{{url}}">{{buttonText}}</a>
        </section>

    {{/each}}

</main>
